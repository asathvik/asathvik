#!/bin/bash
################################################################################
# Purpose:
#	DBA global profile script.
#
# Usage:
# You can add it to /etc/profile.local, by running the command below (exclude #):
# if ! grep '. /dba_share/scripts/bash/profile/dba_global_profile.sh' /etc/profile.local; then echo '. /dba_share/scripts/bash/profile/dba_global_profile.sh' >> /etc/profile.local; fi
# Source /etc/profile.local to load the profile right away:
# . /etc/profile.local
#


# Global alias commands:
alias cdwin='. /dba_share/scripts/bash/utils/cdwin.sh'
alias runbash='/dba_share/scripts/bash/utils/run_bash_script_anywhere.sh'
alias runmysql='/dba_share/scripts/mysql/utils/run_mysql_script_anywhere.sh'
